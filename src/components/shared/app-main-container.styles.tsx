import { css } from '@emotion/react';

export const StyledAppDefaultMarginsClasses = 'mx-auto max-w-7xl px-[12px] sm:px-[24px] lg:px-[96px]';

export const StyledAppMainContainer = css({
    minHeight: '100vh',
});

export const StyledAppMainContainerWrapper = css({
    //overflowX: 'hidden',
});
